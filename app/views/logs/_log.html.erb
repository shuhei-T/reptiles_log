<li class="timeline-item bg-light rounded ms-3 p-4 shadow">
  <div class="timeline-arrow"></div>
  <h2 class="h5 mb-0">title</h2><span class="small text-muted"><i class="far fa-clock me-1"></i></i><%= l log.created_at, format: :long %></span>
  <section>
    <%= link_to reptile_log_path(@reptile, log), method: :delete, data: { confirm: "#{l log.created_at, format: :long}の記録を削除しますか？" } do %>
    <i class="fas fa-trash"></i>
    <span>削除</span>
    <% end %>
    <p class="text-small mt-2 fw-light">Loem ipsum dolor sit amet, consectetur</p>
  </section>
  <div class="log_header">
  </div>
</li>


<%= log.id %>
<%= log.remark %>
<%= log.condition %>
<%= log.shit %>
<%= log.bath %>
<%= log.handling %>
<%= log.creaning %>
<%= log.sheding %>
<%= log.weight %>
<%= log.length %>
<%= log.temperature %>
<%= log.humidity %>

