<% content_for(:title, t('.title')) %>
<div class="container reptile_new">
  <h1><%= t('.title') %></h1>
  <div class="col-12">
      <div class="row">
      <%= render 'form', { reptile: @reptile } %>
      </div>
  </div>
</div>