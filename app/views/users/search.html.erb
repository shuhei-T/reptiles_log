<% content_for(:title, t('.title')) %>
<div class="container mb-5 pt-2 user-index">
  <h1><%= t('.title') %></h1>
  <div class="row search_wrapper">
    <div class="col-md-12">
        <%= render partial: 'user', collection: @search_users %>
    </div>
  </div>
  <!-- ページネーション -->
  <div class="row">
    <div class="col-md-12">
      <%= paginate @search_users, theme: 'bootstrap-5' %>
    </div>
  </div>
</div>
